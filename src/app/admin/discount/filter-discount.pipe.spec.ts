import { FilterDiscountPipe } from './filter-discount.pipe';

describe('FilterDiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
